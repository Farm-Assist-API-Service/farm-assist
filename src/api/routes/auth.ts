import { expressHttpAdapter } from "../adapters/express.adapter";
import { Router } from "../../interfaces";

export default function authRoutes(router: Router) {
    router
        // .post('/register', expressHttpAdapter())
    return router;
}