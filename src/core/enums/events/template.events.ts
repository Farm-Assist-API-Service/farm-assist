export enum TemplateEvents {
  NEW = 'template.new',
  NEW_CARD = 'template.card.new',
  CREATED = 'template.created',
  UPDATED = 'template.updated',
  DELETED = 'template.deleted',
  GENERATE = 'template.generate',
}
